import styles from './styles/Footer.module.css'

const Footer = () => {
    return (
      <div className={`${styles.footer} flex`}>
        <h1>Footer</h1>
      </div>
    );
}

export default Footer;